/**
 * Design-system folders (scaffold only).
 *
 * - buttons/     — brand button compositions (wrap ui/button)
 * - cards/       — card patterns for services, testimonials, etc.
 * - navigation/  — header / mobile nav compositions
 * - footer/      — site footer compositions
 * - forms/       — form field layouts (RHF + Zod + ui primitives)
 * - typography/  — Heading, Text, Lead, etc.
 * - animations/  — Framer Motion presets & wrappers
 * - icons/       — curated icon wrappers around lucide-react
 *
 * Primitives live in ui/ (shadcn). Feature-specific UI lives in features/.
 */

export {};
